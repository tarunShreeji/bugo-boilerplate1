---
label: Menu - Main
hide_body: true
fields:
- name: parent
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: pages
  label: Parent
  description: Select a menu parent. (optional)
- name: weight
  type: number
  label: Weight
  description: 'Add a page weight to determine it''s position in the menu. Note: you
    can use negative numbers. '
  default: 0
  required: true
  config:
    min: 
    max: 
    step: 
