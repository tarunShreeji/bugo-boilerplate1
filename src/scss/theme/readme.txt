# Theme SCSS files

The files are organized in a way that allows you to change the entire theme or just add some custom styles.

The custom folder is compiles into a single file named custom.css

The theme folder is used for theme varaibles, overriding bootsrap variables and defining the styles for the theme. The folder will result in a file named theme.css.

The mixins is for storing your mixins.
